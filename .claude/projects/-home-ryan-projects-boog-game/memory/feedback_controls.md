---
name: correct_controls
description: Q key is discard/drop, not use item. I key is use item. Don't confuse them.
type: feedback
---

Q key = drop/discard item, NOT use item. I key = use consumable/chant.
**Why:** User corrected this — the MEMORY.md had incorrect control mappings listing Q as "use".
**How to apply:** When describing controls, always verify against the actual key bindings in Player.ts rather than relying on memory notes.
